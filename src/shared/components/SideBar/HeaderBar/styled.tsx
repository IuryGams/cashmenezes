"use client"
import styled from "styled-components"

const ContainerUserSection = styled.header`
    display: grid;
    grid-template-columns: 40% 60%;
    grid-template-rows: 50% 50%;
    grid-template-areas:
    "image" "name"
    "image" "profession";

`

// const 

export {
    ContainerUserSection
}